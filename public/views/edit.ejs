<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Addon</title>
    <link rel="stylesheet" href="/create-an-addon/style-create.css">
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400&display=swap" rel="stylesheet">
    <!-- Quill CSS -->
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
</head>
<body>
    <header>
        <div class="logo">
            <img src="/logo.png" alt="Logo Addon" />
            <a href="https://www.youtube.com/@VatonageYT" target="_blank" class="no-style-link">
                <span>vatonage</span>
            </a>
        </div>
        <nav>
            <a href="/site.html" class="home">Home</a>
            <a href="/create.html" class="create">Create</a>
        </nav>
        <div class="cta">
            <span id="username-display" class="username-display">Login</span>
            <button id="logout-btn" style="display:none;">Logout</button>
        </div>
    </header>

    <div class="create-container">
        <h1>Edit Your Addon</h1>
        <form id="edit-addon-form" action="/edit-addon/<%= addon._id %>" method="POST" enctype="multipart/form-data">
            <!-- Nome do Addon -->
            <div class="form-group">
                <label for="addon-name">Addon Name:</label>
                <input type="text" id="addon-name" name="name" value="<%= addon.name %>" required>
            </div>

            <!-- Descrição Simples -->
            <div class="form-group">
                <label for="simple-description">Simple Description:</label>
                <textarea id="simple-description" name="simpleDescription" rows="3" required><%= addon.simpleDescription %></textarea>
            </div>

            <!-- Categoria -->
            <div class="form-group">
                <label for="addon-category">Category:</label>
                <select id="addon-category" name="category" required>
                    <option value="Mechanics" <%= addon.category === 'Mechanics' ? 'selected' : '' %>>Mechanics</option>
                    <option value="Magic" <%= addon.category === 'Magic' ? 'selected' : '' %>>Magic</option>
                    <option value="Adventure" <%= addon.category === 'Adventure' ? 'selected' : '' %>>Adventure</option>
                    <option value="Technology" <%= addon.category === 'Technology' ? 'selected' : '' %>>Technology</option>
                    <option value="Decoration" <%= addon.category === 'Decoration' ? 'selected' : '' %>>Decoration</option>
                </select>
            </div>

            <!-- Descrição Detalhada usando Quill -->
            <div class="form-group">
                <label for="addon-description">Detailed Description:</label>
                <div id="editor-container"><%- addon.description %></div>
                <input type="hidden" id="addon-description" name="description"> <!-- Campo hidden para armazenar a descrição em HTML -->
            </div>

            <!-- Upload de nova Imagem -->
            <div class="form-group">
                <label for="addon-image">Addon Image (optional):</label>
                <input type="file" id="addon-image" name="image" accept="image/*">
                <img src="/<%= addon.image %>" alt="Current Image" width="100">
            </div>

            <!-- Upload de novo Arquivo -->
            <div class="form-group">
                <label for="addon-file">Addon File (optional):</label>
                <input type="file" id="addon-file" name="addonFile" accept=".zip,.mcaddon">
                <p>Current file: <a href="/uploads/<%= addon.file %>"><%= addon.file %></a></p>
            </div>

            <!-- Botão de salvar -->
            <div class="form-group">
                <button type="submit">Save Changes</button>
            </div>
        </form>
    </div>
    
    <!-- Footer -->
    <footer>
        <p>© 2024 Addon Showcase. All rights reserved.</p>
        <a href="https://x.com/vatonage1">Twitter</a>
        <a href="#">Discord</a>
    </footer>

    <!-- Quill JS -->
    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
    
    <!-- Carregar o seu script -->
    <script src="/create-an-addon/script-create.js"></script>
</body>
</html>
